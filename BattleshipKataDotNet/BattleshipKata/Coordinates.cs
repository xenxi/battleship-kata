namespace BattleshipKata
{
    public class Coordinates
    {
        private object x;
        private object y;

        public Coordinates(object x, object y)
        {
            this.x = x;
            this.y = y;
        }
    }
}